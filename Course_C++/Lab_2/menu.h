#ifndef COURSE_C___MENU_H
#define COURSE_C___MENU_H

#include <iostream>

class Menu {
private:

public:

    void create(Vector &vector);
    void sum(Vector &vector);
    void sub(Vector &vector);
    void angle(Vector &vector);
    void output(Vector &vector);
    void len(Vector &vector);
};

#endif
